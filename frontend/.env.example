# API Configuration
EXPO_PUBLIC_API_URL=http://192.168.1.9:3000/api

# Environment
NODE_ENV=development  # development, production

# Note: EXPO_PUBLIC_* variables are automatically exposed to your JavaScript code
# in a React Native/Expo application. Other variables are only available in the
# Node.js environment during build time.

# For local development, you can use a local IP address or localhost
# EXPO_PUBLIC_API_URL=http://localhost:3000/api
